# Передача работы: сквозная аналитика «Прознание / CODDY»

Состояние на 23.07.2026 (вечер). Репозиторий: `Jane3dMp/fb_dashboard`,
ветка `main`. Развёрнута **версия 31** Apps Script (~14:50), идентификатор
развёртывания прежний. В `revenue` появились `by_source` и `by_pipeline` —
альфа-деньги по каждому источнику заявок (включая не-Instagram) и по
воронкам Instagram; страница показывает их в колонках «Выручка» вместо
прежних прочерков. Клиент дедуплицируется внутри строки разреза.
Осторожно: капризный дропдаун версий при деплое однажды молча переиздал
старую версию — выбирать «Новая версия» кликом по ref из дерева
доступности и проверять номер версии в итоговом диалоге.

## Цель

Ответить на вопрос «окупается ли реклама в Meta» и в каком направлении.
Цепочка: расход Meta → заявка в amoCRM → оплата в AlfaCRM, с разрезом
по направлениям (каникулы отдельно от CODDY).

## Главное: мост починен, деньги настоящие

На окне 90 дней (24.04–23.07) страница «Путь клиента» показывает:
расход в Instagram **$3 117**, оплативших **103**, выручка **59 502 BYN**,
стоимость клиента **88 BYN**, **ROAS 6.58×** (по курсу FX_RATE 2.9).
По направлениям: Уикенд 50 122 · CODDY+Прознание 8 250 · Детали 1 130.

Это **нижняя граница**: с Альфой связано 12% заявок (146 из 1219) — у 78%
заявок из Instagram в amo нет телефона (переписка без обмена номером).
Из заявок С телефоном находятся в Альфе 54%.

**Как устроен мост** (`pplAlfaRevenueCore_` в `apps-script/people.gs`,
чистое ядро, гоняется в Node):
1. `alfa_url` из карточки amo — пока пуст у всех, но если начнут заполнять,
   подхватится сам и имеет приоритет;
2. телефон: `phone_e164` заявки против нормализованных номеров клиента
   Альфы из листа `RAW_alfa_customers`. Нормализация одинаковая с двух
   сторон (`pplNormPhone_` = `normalizePhone_` из etl_amo).
Семья (несколько детей на один номер) считается одним оплатившим,
деньги каждого клиента — один раз, платежи только с даты заявки.

**Проверено тройной сверкой**: локальная симуляция на боевых выгрузках
(`node simulate.js` в scratchpad прошлой сессии) → живой JSON → страница.
Цифры совпали до копейки (59 501.50).

## Что вскрылось и починено по дороге

**Старая выгрузка платежей была сломана трижды** (файл `Etl alpha.gs`,
больше НЕ используется, триггеры сняты):
1. Фильтр дат передавался как `document_date_from/to` — Альфа эти имена
   молча игнорирует (рабочие: `date_from`/`date_to`). Каждый «месяц»
   тянул ВСЮ историю с 2022 года.
2. Отметка месяца в `RAW_pays_meta` сравнивала строку '2026-07' с Date
   из ячейки — никогда не совпадала, текущий месяц грузился заново каждый
   день. Итог: в RAW_pays лежало 76.6 тыс. строк — история в ~3 экземплярах.
3. `ALFA_BRANCHES` был '1,2' — платежи Каникул (филиал 4, 867 шт.) и
   Интенсивов (филиал 5, 72 шт.) не выгружались вовсе. Свойство теперь
   **'1,2,4,5'**.

**Филиал 3 («Якубовского, 90») — зеркало филиала 1**: те же клиенты и те
же платежи с теми же id (проверено по API). В ALFA_BRANCHES его добавлять
НЕЛЬЗЯ — всё задвоится. Дедуп по pay_id в коде на этот случай есть.

**Новая схема платежей**: `pplRebuildPays()` (в people.gs) ежедневно
пересобирает лист RAW_pays целиком: только доходы (`pay_type_id: 1`
серверный фильтр + страховка в коде), дедуп по pay_id, защита от пустого
ответа (< 1000 платежей — лист не трогается). Сейчас в листе **28 730**
строк — ровно столько, сколько отдаёт API. Прогон занимает ~6 минут — на
грани лимита; если начнёт падать по таймауту, разнести филиалы по двум
триггерам (1 отдельно, остальные вместе).

**Новый лист `RAW_alfa_customers`** (заполняет `pplEtlAlfaCustomers()`,
1712 записей — **с `is_study: 2`**, иначе customer/index молча отдаёт
только учеников и теряет лидов Альфы, а записанные на пробное часто ещё
лиды; нашла владелица на живом примере #4994, после правки связано
+19 заявок и +4 оплативших): customer_id, branches, phones (нормализованные, через `;`),
amo_contact_id (из стандартного поля `web` Альфы — заполнен у ~18%,
пригодится для точной сверки, если в RAW_leads появятся id контактов),
created_at.

**Мёртвые поля, на которые не надеяться** (проверено по API):
поле контакта amo «(X) Ссылка на alfaCRM» (id 1652617) пусто у всех
заявок; обратное поле Альфы `custom_url_amo_client` пусто у всех клиентов.

## Триггеры (пересобраны, ставит pplSetupDailyTriggers)

4 `pplEtlAlfaCustomers` · 5 `runAmoEtl` · 6 `pplRebuildPays` ·
8 `buildMart` · 9 `buildChannel` · 10 `buildBrands` ·
11 `buildKanikulySverka`

`backfillAlpha` и `dumpAlfaLinks` из расписания выведены (список retired
в pplSetupDailyTriggers — при повторном запуске их триггеры сносятся).
`RAW_pays_meta` больше никем не пишется и не читается — можно удалить лист.

## Ключевые факты, чтобы не искать заново

**Проект Apps Script «ФБ»**
`1ry17Mvsq94L-pzIxfytfFiFxkkr8euzVcei1B6bs1C0sCbufw00Qw036`
Идентификатор развёртывания (URL `/exec` не менялся ни разу):
`AKfycbwUcboOjB-_cLVnh549OU6BLK43F4rBn6QDN6eVohKfc5YAcNP2T-K_KVRqKm4_iHg4`

**Таблица** `SHEET_ID` = `1OTVqk6YDFZQXuKxYRnYTMAOmlrUk__pJei03JqthVkE`
Листы: `RAW_leads`, `RAW_pays`, `RAW_alfa_customers`, `KANIKULY_SVERKA`,
`MART_*`, `RAW_pays_meta` (мёртв).

**`RAW_leads`** (пишет `etl_amo.gs`): `created_at, phone_e164, source,
utm_source, utm_campaign, utm_content, fbclid, pipeline, stage, alfa_url,
lead_id`

**`RAW_pays`** (пишет `pplRebuildPays` из people.gs): `document_date,
customer_id, income, branch, pay_item_id, payer_name, pay_id`

**Свойства скрипта**: SHEET_ID, AMO_SUBDOMAIN, AMO_TOKEN, FB_TOKEN,
AD_ACCOUNTS (`id:Название,...`, два кабинета), DASH_KEY, FX_RATE (2.9),
ALFA_HOST (proznanie4eee.s20.online), ALFA_EMAIL, ALFA_APIKEY,
ALFA_BRANCHES (**1,2,4,5** — филиал 3 не добавлять, зеркало).

**Группировка направлений** — `BRAND_MAP` в `build_brands.gs`, общая
глобальная область. Каникулы → «Уикенд»; Абонементы/Регулярные/
Перспективные/Интенсивы → «CODDY + Прознание»; отдельно Детали,
Нейропсихолог, Мастерская; «Тест» исключён. Свою копию не заводить
(в Node-тестах — уменьшенная копия в sandbox, это осознанно).

**API Альфы**: POST `https://HOST/v2api/{branch}/{entity}/index`,
заголовок `X-ALFACRM-TOKEN` (логин `/v2api/auth/login`, email+api_key).
Страница 50 записей, `total` в ответе. Фильтр платежей по датам:
`date_from`/`date_to` (ДД.ММ.ГГГГ). `pay_type_id: 1` = Доход.

## Решения, которые лучше не пересматривать без причины

- **Платежи считаются с даты заявки**; период — по дате заявки, не
  платежа; один клиент один раз; доля связанных на странице. Всё как было.
- **Полный пересбор RAW_pays вместо инкремента** — дёшево (6 мин/день),
  самовосстанавливается, нет учёта месяцев, который уже один раз наврал.
- **Хелперы people.gs с префиксом ppl** — общая глобальная область,
  коллизии имён уже случались.

## Как деплоить (проверенный путь без мучений с буфером)

1. Правки локально → коммит → `git push` (repo = источник и для Pages).
2. В редакторе Apps Script (вкладка Chrome с аккаунтом proznanie):
   через javascript в консоли страницы записать файл прямо в Monaco:
   модель people.gs находится по `monaco.editor.getModels()` (798→1021
   строк была её длина), `fetch` raw.githubusercontent + `setValue`.
   Затем Ctrl+S, проверить «Сохранено на Диске» и что в списке функций
   появились новые.
3. «Начать развертывание → Управление развертываниями → карандаш →
   Версия: Новая → Развернуть» — id развёртывания сохраняется.
4. Проверка: `/exec?view=people&days=90&nocache=1&key=<DASH_KEY>`.
Капризы редактора (залипание списка функций, срыв вставки из буфера)
описаны в старых заметках ниже — с Monaco-способом они не мешают.

## Вебхук Instagram: секрет заменён, подписка вписана в Meta

Сделано 23.07 после обеда (версия 30 «ФБ» — попутно):

- `URL_SECRET` в проекте «IG webhook» заменён на случайный (значение —
  в свойствах того проекта; в чатах и документах его больше нет).
  `VERIFY_TOKEN` не менялся. Свойства применяются без передеплоя.
- Выяснилось, что Callback URL в Meta до сих пор **не был вписан вообще**
  («настроен полностью» в прошлой передаче описывало готовность кода):
  проверены оба приложения и все четыре вебхук-раздела — пусто.
- Вписан и верифицирован (зелёная галка): приложение **Proznanie**
  (708933311750084) → Сценарии использования → API Instagram → шаг
  «3. Настройте Webhooks». URL = `/exec`-адрес проекта «IG webhook»
  с `?s=<новый секрет>`; маркер = VERIFY_TOKEN из свойств.
- Помнить: события пойдут только после публикации приложения (App Review),
  Meta прямо пишет это над формой.
- В RAW_leads добавлена колонка `contact_id` (etl_amo.gs теперь лежит в
  репозитории точной копией), и в ядре моста появился путь «id контакта
  amo ↔ amo_contact_id клиента Альфы» с приоритетом выше телефона.
  Прирост связанных пока +1 заявка (контактная ссылка есть у ~18%
  клиентов, и почти все они уже ловились телефоном) — это страховка
  на будущее и точность при семьях с общим номером.

## Дальше по плану

1. **Поднять долю связанных заявок** (сейчас 12%). Главный рычаг —
   процесс: научить менеджеров/робота amo заполнять телефон контакта у
   Instagram-заявок (без телефона мост бессилен). Технически: IGSID
   после App Review.
   **Проверено 23.07 диагностикой `pplDumpContactFields`** (40 заявок без
   phone_e164 — свежие и старые): у их контактов в amo нет НИ ОДНОГО
   заполненного телефоноподобного поля, в полях сделок только служебный
   base64 Facebook (`ecf_…` = lead_allocation), в примечаниях сделок
   номеров тоже нет. Код выгрузки не виноват — номеров в amo просто нет.

   **Уточнение процесса от владельца + разбор по стадиям**: телефон и не
   должен быть у всех — переписка из Instagram идёт без номера, телефон
   появляется на этапе передачи в Альфу («Записан на пробное»,
   «Абонемент 26/27» и т.п.). Данные это подтверждают: на зрелых стадиях
   покрытие телефоном ~100% (На абонементе 72/72, Внесена предоплата
   61/61, Оплачено 15/15), а 65% всех заявок — «Закрыто и не
   реализовано», где телефона нет и не будет (164 из 1131). Поэтому
   «связано 12% всех заявок» — норма, а метрика здоровья — «связано из
   заявок С телефоном» (сейчас ~54%; сноска на странице переписана под
   это 23.07). Точечная дыра для менеджеров: у «Закрыли абонемент» в
   Альфе находятся 2 из 7, у «Успешно реализовано» 59 из 64 с телефоном —
   у остальных номер в amo не совпадает с номером в Альфе (разные
   родители). Список таких сделок можно выгрузить той же диагностикой.
2. **App Review в Meta** — за владельцем. Тексты в `docs/meta-app-review.md`.
   Гейт подтверждён вживую: «URL Политики конфиденциальности» в настройках
   приложения Proznanie пуст — без него заявку не подать.
3. **Переезд с Apps Script на `app.proznanie.club`** — схема в репозитории
   `finmodel-club` (PHP `api/`, деплой GitHub Actions по FTPS, секретный
   `config.php` руками на сервере). В `assets/dash.js` меняется один адрес.

## Тесты

```
node tests/logic.test.js
```

58 проверок: склейка по IGSID и времени, сводка по объявлениям, валюты,
воронки, разбор ссылки на Альфу, нормализация телефона, даты из ячеек
(включая Date из чужого realm — поэтому в `pplAnyIso_` утиная типизация,
не instanceof), ядро выручки: мост по телефону, семья, дубли платежей,
приоритет ссылки, границы периода.
